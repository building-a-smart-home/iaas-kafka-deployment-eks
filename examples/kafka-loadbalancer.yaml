apiVersion: v1
kind: Service
metadata:
  name: alco-cluster-kafka-bootstrap
  namespace: kafka
spec:
  selector:
    app: kafka
  ports:
    - name: kafka
      port: 9092
      targetPort: 9092
    - name: kafka-admin
      port: 9093
      targetPort: 9093
  type: LoadBalancer
